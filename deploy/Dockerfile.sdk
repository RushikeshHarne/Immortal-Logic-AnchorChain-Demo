FROM python:3.11-slim
WORKDIR /app
COPY sdk/requirements.txt .
RUN pip install -r requirements.txt
COPY sdk /app/sdk
ENV PYTHONPATH=/app/sdk/src
CMD ["python", "-m", "immortal_logic.drills.epls_demo"]
