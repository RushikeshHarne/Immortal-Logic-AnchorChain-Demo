# Integration Guide (Sandbox)
- Install Docker Desktop
- `docker compose -f deploy/docker-compose.yml up --build`
- Run drills via `make -C sdk drill-dry` then `drill-onchain` (when RPC + contract set)
